{
  "name": "waclient",
  "version": "1.0.0",
  "description": "WhatsApp Business API Client for Node.js",
  "scripts": {
    "build": "tsc && cp -r public/* dist/",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/node": "^4.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "dotenv": "^17.2.3"
  }
}
